[package]
name = "rebellion2-update-core"
version = "0.1.0"
edition = "2021"
description = "Manifest diff + content-addressed apply engine for the Rebellion II launcher."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"

[dev-dependencies]
tempfile = "3"
